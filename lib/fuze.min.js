/**
 * A jquery, future, dirt simple js template engine
 *
 * @todo Add more options, such as callback functions to be called when an individual item is fuzed
 *
 * Fuze v0.3
 * Copyright (C) 2011 Sentient Matter
 * Licensed under the MIT license
 */
(function($){var methods={doFuze:function(data,options){var self=this;self.each(function(){$.each(data,function(key,item){var selector='#'+key+', '+'.'+key;if($.isArray(item)){}else if(item instanceof Object){if(item.toJSON&&typeof(item.toJSON)=='function'){item=item.toJSON();}
options.asynchronous=false;$(selector,self).fuze(item,options);}else if(typeof(item)=='string'||typeof(item)=='number'||typeof(item)=='boolean'){self.find(selector).html(item);self.find('[name='+key+']').val(item);}});});}}
$.fn.fuze=function(data,options){var deferred=$.Deferred();var self=this;var defaults={asynchronous:true}
options||(options={});options=$.extend(defaults,options);if(options.asynchronous){setTimeout(function(){methods.doFuze.apply(self,[data,options]);deferred.resolveWith(self);},0);}else{methods.doFuze.apply(self,[data,options]);deferred.resolveWith(self);}
return deferred.promise(this);};})(jQuery)